import DevSpaceClient from "@/components/DevSpaceClient";

/**
 * DevSpace Landing Page - Server Component
 *
 * Static landing page for the DevSpace product, modeled on the standalone HTML
 * in `Helper Resources/landing-page/` but rebuilt to match the portfolio's
 * design system (glassmorphism, JetBrains Mono, motion, accessibility).
 */
export default function DevSpacePage() {
  return <DevSpaceClient />;
}
